# Style Tokens
# Style Tokens use primitive tokens to define the style of the design system.

# Primary
style-tokens:
  primary:
    description: "Primary color is used to create a sense of trust and reliability."
    values:
      50: "#f1f7fe"
      100: "#e3ecfb"
      200: "#c0d9f7"
      300: "#88bbf1"
      400: "#4998e7"
      500: "#227bd5"
      600: "#1565c0"
      700: "#114c93"
      800: "#12427a"
      900: "#153965"
      950: "#0e2443"
  secondary:
    description: "Secondary color is used to create a sense of action and urgency."
    values:
      50: "#faf6fe"
      100: "#f2ebfc"
      200: "#e7dafa"
      300: "#d5bdf5"
      400: "#bc93ed"
      500: "#a269e3"
      600: "#8a48d3"
      700: "#7737b9"
      800: "#643297"
      900: "#52297a"
      950: "#361259"
  success:
    description: "Success color is used to create a sense of completion and satisfaction."
    values:
      50: "#f1f8f2"
      100: "#dcefdc"
      200: "#bcdebe"
      300: "#90c597"
      400: "#67aa72"
      500: "#3f8a4e"
      600: "#2e6d3c"
      700: "#255731"
      800: "#1f4629"
      900: "#1a3a22"
      950: "#0e2014"
  warning:
    description: "Warning color is used to create a sense of caution and attention."
    values:
      50: "#fbf8ef"
      100: "#f3e9d2"
      200: "#e7d0a0"
      300: "#dab46f"
      400: "#d29d4d"
      500: "#c78037"
      600: "#b0642f"
      700: "#934a2a"
      800: "#793c27"
      900: "#643323"
      950: "#381910"
  danger:
    description: "Danger color is used to create a sense of error and danger."
    values:
      50: "#fdf4f3"
      100: "#fce7e7"
      200: "#f8d3d4"
      300: "#f3aeb1"
      400: "#eb8186"
      500: "#df545f"
      600: "#ca3447"
      700: "#bb2a40"
      800: "#8e2336"
      900: "#7a2134"
      950: "#440d18"
  neutral:
    description: "Neutral color is used to create a sense of neutrality and balance."
    values:
      50: "#f5f8fa"
      100: "#e0e6eb"
      200: "#c2cdd6"
      300: "#a3b4c2"
      400: "#859aad"
      500: "#668199"
      600: "#52677a"
      700: "#3d4e5c"
      800: "#29343d"
      900: "#141a1f"
      950: "#0a0d0f"

  # Background
  # Background colors are used as the default background for the overall design system.
  background:
    subtle:
      description: "A subtle background color used to create a sense of calm and focus."
      values:
        light: "var(--neutral-50)"
        dark: "var(--neutral-950)"
    default:
      description: "The default background color used for the design system."
      values:
        light: "var(--neutral-100)"
        dark: "var(--neutral-900)"

  # Surface
  # Surface colors are used over the background of the design system
  surface:
    subtle:
      description: "A subtle surface color used for the main app. Use this color for cards, modals, and other surfaces that should not draw attention"
      values:
        light: "var(--neutral-100)"
        dark: "var(--neutral-900)"
    default:
      description: "A default surface color used for the main app. Use this color for cards, modals, and other surfaces that should draw attention"
      values:
        light: "var(--neutral-200)"
        dark: "var(--neutral-800)"

  # Accent
  # Accent colors are used over the surface and background of the design app. This can be used to color components or specific parts of the UI
  accent:

    # Neutral
    neutral:
      description: "A default neutral accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-600)"
        dark: "var(--neutral-300)"
    neutral-hover:
      description: "A hover neutral accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-200)"
    neutral-active:
      description: "An active neutral accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-100)"
    neutral-negative:
      description: "A negative neutral accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-300)"
        dark: "var(--neutral-700)"
    neutral-negative-hover:
      description: "A hover negative neutral accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-200)"
        dark: "var(--neutral-800)"
    neutral-negative-active:
      description: "An active negative neutral accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-100)"
        dark: "var(--neutral-900)"
    neutral-subtle:
      description: "A subtle neutral accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-50)"
        dark: "var(--neutral-950)"
    neutral-subtle-hover:
      description: "A hover subtle neutral accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-100)"
        dark: "var(--neutral-900)"
    neutral-subtle-active:
      description: "An active subtle neutral accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-200)"
        dark: "var(--neutral-800)"

    # Primary
    primary:
      description: "A default primary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-500)"
        dark: "var(--primary-500)"
    primary-hover:
      description: "A hover primary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-600)"
        dark: "var(--primary-400)"
    primary-active:
      description: "An active primary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-700)"
        dark: "var(--primary-300)"
    primary-negative:
      description: "A negative primary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-500)"
        dark: "var(--primary-500)"
    primary-negative-hover:
      description: "A hover negative primary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-400)"
        dark: "var(--primary-600)"
    primary-negative-active:
      description: "An active negative primary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-300)"
        dark: "var(--primary-700)"
    primary-subtle:
      description: "A subtle primary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-50)"
        dark: "var(--primary-950)"
    primary-subtle-hover:
      description: "A hover subtle primary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-100)"
        dark: "var(--primary-900)"
    primary-subtle-active:
      description: "An active subtle primary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-200)"
        dark: "var(--primary-800)"
    primary-disabled:
      description: "A disabled primary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-200)"
        dark: "var(--primary-800)"
    
    # Secondary
    secondary:
      description: "A default secondary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-500)"
        dark: "var(--secondary-500)"
    secondary-hover:
      description: "A hover secondary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-600)"
        dark: "var(--secondary-400)"
    secondary-active:
      description: "An active secondary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-700)"
        dark: "var(--secondary-300)"
    secondary-negative:
      description: "A negative secondary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-500)"
        dark: "var(--secondary-500)"
    secondary-negative-hover:
      description: "A hover negative secondary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-400)"
        dark: "var(--secondary-600)"
    secondary-negative-active:
      description: "An active negative secondary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-300)"
        dark: "var(--secondary-700)"
    secondary-subtle:
      description: "A subtle secondary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-50)"
        dark: "var(--secondary-950)"
    secondary-subtle-hover:
      description: "A hover subtle secondary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-100)"
        dark: "var(--secondary-900)"
    secondary-subtle-active:
      description: "An active subtle secondary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-200)"
        dark: "var(--secondary-800)"
    secondary-disabled:
      description: "A disabled secondary accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-200)"
        dark: "var(--secondary-800)"

    # Success
    success:
      description: "A default success accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-500)"
        dark: "var(--success-500)"
    success-hover:
      description: "A hover success accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-600)"
        dark: "var(--success-400)"
    success-active:
      description: "An active success accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-700)"
        dark: "var(--success-300)"
    success-negative:
      description: "A negative success accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-500)"
        dark: "var(--success-500)"
    success-negative-hover:
      description: "A hover negative success accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-400)"
        dark: "var(--success-600)"
    success-negative-active:
      description: "An active negative success accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-300)"
        dark: "var(--success-700)"
    success-subtle:
      description: "A subtle success accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-50)"
        dark: "var(--success-950)"
    success-subtle-hover:
      description: "A hover subtle success accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-100)"
        dark: "var(--success-900)"
    success-subtle-active:
      description: "An active subtle success accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-200)"
        dark: "var(--success-800)"
    success-disabled:
      description: "A disabled success accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-200)"
        dark: "var(--success-800)"

    # Warning
    warning:
      description: "A default warning accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-500)"
        dark: "var(--warning-500)"
    warning-hover:
      description: "A hover warning accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-600)"
        dark: "var(--warning-400)"
    warning-active:
      description: "An active warning accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-700)"
        dark: "var(--warning-300)"
    warning-negative:
      description: "A negative warning accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-500)"
        dark: "var(--warning-500)"
    warning-negative-hover:
      description: "A hover negative warning accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-400)"
        dark: "var(--warning-600)"
    warning-negative-active:
      description: "An active negative warning accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-300)"
        dark: "var(--warning-700)"
    warning-subtle:
      description: "A subtle warning accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-50)"
        dark: "var(--warning-950)"
    warning-subtle-hover:
      description: "A hover subtle warning accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-100)"
        dark: "var(--warning-900)"
    warning-subtle-active:
      description: "An active subtle warning accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-200)"
        dark: "var(--warning-800)"
    warning-disabled:
      description: "A disabled warning accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-200)"
        dark: "var(--warning-800)"

    # Danger
    danger:
      description: "A default danger accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-500)"
        dark: "var(--danger-500)"
    danger-hover:
      description: "A hover danger accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-600)"
        dark: "var(--danger-400)"
    danger-active:
      description: "An active danger accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-700)"
        dark: "var(--danger-300)"
    danger-negative:
      description: "A negative danger accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-500)"
        dark: "var(--danger-500)"
    danger-negative-hover:
      description: "A hover negative danger accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-400)"
        dark: "var(--danger-600)"
    danger-negative-active:
      description: "An active negative danger accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-300)"
        dark: "var(--danger-700)"
    danger-subtle:
      description: "A subtle danger accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-50)"
        dark: "var(--danger-950)"
    danger-subtle-hover:
      description: "A hover subtle danger accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-100)"
        dark: "var(--danger-900)"
    danger-subtle-active:
      description: "An active subtle danger accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-200)"
        dark: "var(--danger-800)"
    danger-disabled:
      description: "A disabled danger accent color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-200)"
        dark: "var(--danger-800)"

  border:

    # Neutral Border
    neutral:
      description: "A default neutral border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-300)"
        dark: "var(--neutral-700)"
    neutral-hover:
      description: "A hover neutral border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-400)"
        dark: "var(--neutral-600)"
    neutral-active:
      description: "An active neutral border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-500)"
        dark: "var(--neutral-500)"
    neutral-negative:
      description: "A negative neutral border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"
    neutral-negative-hover:
      description: "A hover negative neutral border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-600)"
        dark: "var(--neutral-400)"
    neutral-negative-active:
      description: "An active negative neutral border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--neutral-500)"
        dark: "var(--neutral-500)"

    # Primary Border
    primary:
      description: "A default primary border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-500)"
        dark: "var(--primary-500)"
    primary-hover:
      description: "A hover primary border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-600)"
        dark: "var(--primary-400)"
    primary-active:
      description: "An active primary border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-700)"
        dark: "var(--primary-300)"
    primary-negative:
      description: "A negative primary border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-500)"
        dark: "var(--primary-500)"
    primary-negative-hover:
      description: "A hover negative primary border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-400)"
        dark: "var(--primary-600)"
    primary-negative-active:
      description: "An active negative primary border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--primary-300)"
        dark: "var(--primary-700)"

    # Secondary Border
    secondary:
      description: "A default secondary border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-500)"
        dark: "var(--secondary-500)"
    secondary-hover:
      description: "A hover secondary border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-600)"
        dark: "var(--secondary-400)"
    secondary-active:
      description: "An active secondary border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-700)"
        dark: "var(--secondary-300)"
    secondary-negative:
      description: "A negative secondary border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-500)"
        dark: "var(--secondary-500)"
    secondary-negative-hover:
      description: "A hover negative secondary border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--secondary-400)"
        dark: "var(--secondary-600)"

    # Success Border
    success:
      description: "A default success border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-500)"
        dark: "var(--success-500)"
    success-hover:
      description: "A hover success border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-600)"
        dark: "var(--success-400)"
    success-active:
      description: "An active success border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-700)"
        dark: "var(--success-300)"
    success-negative:
      description: "A negative success border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-500)"
        dark: "var(--success-500)"
    success-negative-hover:
      description: "A hover negative success border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-400)"
        dark: "var(--success-600)"
    success-negative-active:
      description: "An active negative success border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--success-300)"
        dark: "var(--success-700)"
    
    # Warning Border
    warning:
      description: "A default warning border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-500)"
        dark: "var(--warning-500)"
    warning-hover:
      description: "A hover warning border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-600)"
        dark: "var(--warning-400)"
    warning-active:
      description: "An active warning border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-700)"
        dark: "var(--warning-300)"
    warning-negative:
      description: "A negative warning border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-500)"
        dark: "var(--warning-500)"
    warning-negative-hover:
      description: "A hover negative warning border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-400)"
        dark: "var(--warning-600)"
    warning-negative-active:
      description: "An active negative warning border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--warning-300)"
        dark: "var(--warning-700)"
    
    # Danger Border
    danger:
      description: "A default danger border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-500)"
        dark: "var(--danger-500)"
    danger-hover:
      description: "A hover danger border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-600)"
        dark: "var(--danger-400)"
    danger-active:
      description: "An active danger border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-700)"
        dark: "var(--danger-300)"
    danger-negative:
      description: "A negative danger border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-500)"
        dark: "var(--danger-500)"
    danger-negative-hover:
      description: "A hover negative danger border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-400)"
        dark: "var(--danger-600)"
    danger-negative-active:
      description: "An active negative danger border color used on top of surfaces/background. Used for things like buttons, links, and other interactive elements."
      values:
        light: "var(--danger-300)"
        dark: "var(--danger-700)"

    # Other
    focus-outline:
      description: "The color of the focus outline used for the design system."
      values:
        light: "var(--primary-500)"
        dark: "var(--primary-500)"

  # Text Styles
  text:
    default:
      description: "The default text color used for the design system."
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    subtle:
      description: "A subtle text color used for the design system."
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"
    disabled:
      description: "A muted text color used for the design system."
      values:
        light: "var(--neutral-500)"
        dark: "var(--neutral-500)"
    inverse:
      description: "An inverse text color used for the design system."
      values:
        light: "var(--neutral-100)"
        dark: "var(--neutral-900)"
    link:
      description: "A link text color used for the design system."
      values:
        light: "var(--primary-500)"
        dark: "var(--primary-500)"
    link-hover:
      description: "A hover link text color used for the design system."
      values:
        light: "var(--primary-600)"
        dark: "var(--primary-400)"
    link-active:
      description: "An active link text color used for the design system."
      values:
        light: "var(--primary-700)"
        dark: "var(--primary-300)"
    link-visited:
      description: "A visited link text color used for the design system."
      values:
        light: "var(--primary-700)"
        dark: "var(--primary-300)"
    link-disabled:
      description: "A disabled link text color used for the design system."
      values:
        light: "var(--neutral-500)"
        dark: "var(--neutral-500)"

    # On Neutral / Neutral Text
    on-neutral:
      description: "A text color on a neutral background"
      values:
        light: "var(--neutral-50)"
        dark: "var(--neutral-800)"
    on-neutral-hover:
      description: "A text color on a hover neutral background"
      values:
        light: "var(--neutral-100)"
        dark: "var(--neutral-900)"
    on-neutral-active:
      description: "A text color on an active neutral background"
      values:
        light: "var(--neutral-200)"
        dark: "var(--neutral-950)"
    on-neutral-disabled:
      description: "A text color on a disabled neutral background"
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    on-neutral-subtle-subtle:
      description: "A subtle text color on a subtle neutral background"
      values:
        light: "var(--neutral-400)"
        dark: "var(--neutral-700)"
    neutral:
      description: "A neutral (default) text color used for the design system."
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    neutral-hover:
      description: "A hover neutral text color used for the design system."
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-200)"
    neutral-active:
      description: "An active neutral text color used for the design system."
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"

    # On Primary / Primary Text
    on-primary:
      description: "A text color on a primary background"
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    on-primary-hover:
      description: "A text color on a hover primary background"
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-200)"
    on-primary-active:
      description: "A text color on an active primary background"
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"
    on-primary-disabled:
      description: "A text color on a disabled primary background"
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    on-primary-subtle-subtle:
      description: "A subtle text color on a subtle primary background"
      values:
        light: "var(--neutral-400)"
        dark: "var(--neutral-700)"
    primary:
      description: "A primary (default) text color used for the design system."
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    primary-hover:
      description: "A hover primary text color used for the design system."
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-200)"
    primary-active:
      description: "An active primary text color used for the design system."
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"

    # On Secondary / Secondary Text
    on-secondary:
      description: "A text color on a secondary background"
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    on-secondary-hover:
      description: "A hover text color on a secondary background"
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-200)"
    on-secondary-active:
      description: "An active text color on a secondary background"
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"
    on-secondary-disabled:
      description: "A disabled text color on secondary background"
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    on-secondary-subtle-subtle:
      description: "A subtle text color on a subtle secondary background"
      values:
        light: "var(--neutral-400)"
        dark: "var(--neutral-700)"
    secondary:
      description: "A secondary (default) text color used for the design system."
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    secondary-hover:
      description: "A hover secondary text color used for the design system."
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-200)"
    secondary-active:
      description: "An active secondary text color used for the design system."
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"

    # On Success / Success Text
    on-success:
      description: "A text color on a success background"
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    on-success-hover:
      description: "A hover text color on a success background"
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-200)"
    on-success-active:
      description: "An active text color on a success background"
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"
    on-success-disabled:
      description: "A disabled text color on success background"
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    on-success-subtle-subtle:
      description: "A subtle text color on a subtle success background"
      values:
        light: "var(--neutral-400)"
        dark: "var(--neutral-700)"
    success:
      description: "A success (default) text color used for the design system."
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    success-hover:
      description: "A hover success text color used for the design system."
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-200)"
    success-active:
      description: "An active success text color used for the design system."
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"

    # On Warning / Warning Text
    on-warning:
      description: "A text color on a warning background"
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    on-warning-hover:
      description: "A hover text color on a warning background"
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-200)"
    on-warning-active:
      description: "An active text color on a warning background"
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"
    on-warning-disabled:
      description: "A disabled text color on warning background"
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    on-warning-subtle-subtle:
      description: "A subtle text color on a subtle warning background"
      values:
        light: "var(--neutral-400)"
        dark: "var(--neutral-700)"
    warning:
      description: "A warning (default) text color used for the design system."
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    warning-hover:
      description: "A hover warning text color used for the design system."
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-200)"
    warning-active:
      description: "An active warning text color used for the design system."
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"

    # On Danger / Danger Text
    on-danger:
      description: "A text color on a danger background"
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    on-danger-hover:
      description: "A hover text color on a danger background"
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-200)"
    on-danger-active:
      description: "An active text color on a danger background"
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"
    on-danger-disabled:
      description: "A disabled text color on danger background"
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    on-danger-subtle-subtle:
      description: "A subtle text color on a subtle danger background"
      values:
        light: "var(--neutral-400)"
        dark: "var(--neutral-700)"
    danger:
      description: "A danger (default) text color used for the design system."
      values:
        light: "var(--neutral-900)"
        dark: "var(--neutral-100)"
    danger-hover:
      description: "A hover danger text color used for the design system."
      values:
        light: "var(--neutral-800)"
        dark: "var(--neutral-200)"
    danger-active:
      description: "An active danger text color used for the design system."
      values:
        light: "var(--neutral-700)"
        dark: "var(--neutral-300)"