import React from "react";

function Button() {
    return (
        <button className="text-[--background-default] bg-[--background-subtle]">
            Click Me
        </button>
    );
}

export default Button;
