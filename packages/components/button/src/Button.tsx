import React from "react";

function Button() {
    return (
        <button className="bg-accent-primary text-text-on-primary">
            Click Me
        </button>
    );
}

export default Button;
